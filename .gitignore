# Secrets and local state
.env
.env.*
!.env.example
state/
*.key

# Market data lake, feature cache (data/features/), and the trained-model
# registry (data/models/) -- all derived, all sync to the GPU box, never commit.
# Anchored to the repo root: an unanchored "data/" also matches src/trader/data/,
# silently excluding that whole package from version control.
/data/

# Model artifacts
artifacts/
runs/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Node
node_modules/
frontend/dist/

# Reports
reports/
